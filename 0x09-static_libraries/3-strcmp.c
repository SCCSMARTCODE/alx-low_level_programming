#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strcmp -  function that compares two strings.
 *
 * @s1: first parameter
 * @s2: second parameter
 * Return: strcmp
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
