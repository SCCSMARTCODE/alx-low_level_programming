#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * _abs - computes the absolute value of an integer.
 *
 * @x: the integer
 *
 * Return: absolute value
 */

int _abs(int x)
{

	int y = abs(x);

	printf("%d", y);

	return (y);
}
