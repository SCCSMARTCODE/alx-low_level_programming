#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - returns the length of a string
 *
 * @s: expected
 *
 * Return: length of the string
 */

int _strlen(char *s)
{
	int m =  strlen(s);

	return (m);
}
